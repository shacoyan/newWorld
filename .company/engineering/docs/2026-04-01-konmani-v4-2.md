# 設計書: こんまに v4.2 — 出勤ボタン移動・品目カテゴリ・各種修正

## 概要
出勤ボタン移動 / デフォルト退勤時刻追加 / ログアウトボタン修正 / 品目カテゴリ（キャスト/シャンパン）/ 時刻単位をtime inputに適用

---

## Aチーム担当

### A1: style.css — 各種スタイル追加・修正

**① ログアウトボタン修正（明示的な色指定）**
既存の `.btn-logout-section` を以下に変更:
```css
.btn-logout-section {
  width: 100%;
  padding: 14px;
  border: 1.5px solid #fca5a5;
  border-radius: var(--radius);
  background: #fff5f5;
  color: #e11d48;
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  font-family: inherit;
  transition: all 0.15s;
}
.btn-logout-section:hover { background: #fee2e2; }
```

**② 出勤登録ボタン（今日セクション用）を末尾に追加**
```css
.btn-checkin-today {
  width: 100%;
  padding: 12px;
  background: linear-gradient(135deg, #f472b6, #a78bfa);
  color: white;
  border: none;
  border-radius: var(--radius);
  font-size: 14px;
  font-weight: 700;
  cursor: pointer;
  font-family: inherit;
  margin-top: 8px;
  transition: all 0.15s;
}
.btn-checkin-today:hover { opacity: 0.9; }
.btn-checkin-today:active { transform: scale(0.98); }
```

**③ シャンパントグルボタンを末尾に追加**
```css
.btn-champagne-toggle {
  width: 100%;
  padding: 8px 12px;
  background: var(--secondary-bg);
  border: 1px solid rgba(167,139,250,0.3);
  border-radius: var(--radius);
  color: var(--secondary);
  font-size: 13px;
  font-weight: 600;
  text-align: left;
  cursor: pointer;
  font-family: inherit;
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-top: 8px;
}
```

**④ 品目カテゴリラベルを末尾に追加**
```css
.item-category-label {
  font-size: 11px;
  font-weight: 700;
  color: var(--text-muted);
  letter-spacing: 0.05em;
  margin: 14px 0 6px;
  padding-bottom: 4px;
  border-bottom: 1px solid var(--border);
}
```

---

### A2: common.js — defaultEndTime追加・品目にcategoryフィールド追加

`getDefaultSettings()` を以下に変更:
```js
function getDefaultSettings() {
  return {
    salaryType: 'hourly',
    baseSalary: 200000,
    defaultHourlyRate: 1500,
    defaultStartTime: '',
    defaultEndTime: '',        // 追加
    timeStep: 1,
    payPeriodStart: 1,
    items: [
      { id: 'default-drink', name: 'ドリンク', back: 0, category: 'cast' },
      { id: 'default-shot',  name: 'ショット',  back: 0, category: 'cast' },
      { id: 'default-cheki', name: 'チェキ',    back: 0, category: 'cast' }
    ]
  };
}
```

---

## Bチーム担当

### B1: index.html — 出勤登録ボタンをtoday-sectionに追加

時刻入力グループ（`.time-group` が2つある `.section`）の末尾に追加:
```html
        <button id="checkin-btn" class="btn-checkin-today">出勤登録（デフォルト時刻をセット）</button>
```

具体的には、以下の `<div class="section">` ブロック内の最後（`</div>` の直前）に追加:
```html
      <div class="section">
        <div class="time-group">..出勤・退勤..</div>
        <div class="time-group">..時給..</div>
        <button id="checkin-btn" class="btn-checkin-today">出勤登録（デフォルト時刻をセット）</button>  ← 追加
      </div>
```

---

### B2: index.js — 出勤ボタン移動・renderItemRows更新・timeStep適用・カレンダーのbtn-checkin削除

**① renderCalendar() から btn-checkin を削除**
renderCalendar 内のbtn-checkin関連コードを削除:
```js
// 削除する: checkinBtn の createElement/className/dataset/textContent/setAttribute/appendChild の5行
```

**② calendarGridEl の btn-checkin 関連イベントを削除**
bindEvents 内の以下を削除:
- `if (e.target.classList.contains('btn-checkin')) return;` の行（遷移イベント内のガード）
- btn-checkin専用クリックイベントリスナー全体

**③ DOM refs に checkin-btn を追加**
```js
var checkinBtnEl = document.getElementById('checkin-btn');
```

**④ bindEvents() に checkin-btn のロジックを追加**
```js
if (checkinBtnEl) {
  checkinBtnEl.addEventListener('click', function() {
    var start = data.settings.defaultStartTime || '';
    var end   = data.settings.defaultEndTime   || '';
    if (!start && !end) {
      alert('デフォルト出勤・退勤時刻が設定されていません。設定画面から登録してください。');
      return;
    }
    var rec = ensureRecord(data, todayKey);
    if (start) rec.startTime = start;
    if (end)   rec.endTime   = end;
    persistData(data);
    renderToday();
    renderHeader();
  });
}
```

**⑤ renderToday() 内でtimeStepをtime inputに適用**
`timeStartEl.value = ...` の前に追加:
```js
var step = (data.settings.timeStep || 1) === 15 ? 900 : 60;
timeStartEl.setAttribute('step', step);
timeEndEl.setAttribute('step', step);
```

**⑥ renderItemRows() をカテゴリ対応に更新**

現在の `renderItemRows()` を以下に置き換える:
```js
function renderItemRows() {
  itemRowsEl.innerHTML = '';
  var items = data.settings.items || [];

  if (items.length === 0) {
    var msg = document.createElement('p');
    msg.textContent = '品目がありません。設定から追加してください。';
    msg.style.cssText = 'color:var(--text-muted);font-size:13px;text-align:center;padding:8px 0;';
    itemRowsEl.appendChild(msg);
    return;
  }

  var rec       = data.records[todayKey];
  var castItems = items.filter(function(i) { return i.category !== 'champagne'; });
  var champItems = items.filter(function(i) { return i.category === 'champagne'; });

  function makeRow(item) {
    var count = (rec.items && rec.items[item.id]) || 0;
    var back  = (Number(item.back) || 0) * count;
    var row = document.createElement('div');
    row.className = 'item-count-row';
    row.innerHTML =
      '<span class="item-back-label">' + formatMoney(back) + '</span>' +
      '<span class="item-name">' + escapeHtml(item.name) + '</span>' +
      '<button class="item-dec" data-id="' + item.id + '" aria-label="減らす">−</button>' +
      '<span class="item-count-val" data-id="' + item.id + '">' + count + '</span>' +
      '<button class="item-inc" data-id="' + item.id + '" aria-label="増やす">+</button>';
    return row;
  }

  // キャストメニュー（常時表示）
  castItems.forEach(function(item) {
    itemRowsEl.appendChild(makeRow(item));
  });

  // シャンパン（折りたたみ可能）
  if (champItems.length > 0) {
    var toggleBtn = document.createElement('button');
    toggleBtn.className = 'btn-champagne-toggle';
    toggleBtn.innerHTML = 'シャンパン <span>▼</span>';
    toggleBtn.dataset.open = 'true';

    var champWrap = document.createElement('div');
    champWrap.className = 'champagne-items-wrap';

    champItems.forEach(function(item) {
      champWrap.appendChild(makeRow(item));
    });

    toggleBtn.addEventListener('click', function() {
      var isOpen = this.dataset.open === 'true';
      champWrap.style.display = isOpen ? 'none' : '';
      this.dataset.open = isOpen ? 'false' : 'true';
      this.querySelector('span').textContent = isOpen ? '▶' : '▼';
    });

    itemRowsEl.appendChild(toggleBtn);
    itemRowsEl.appendChild(champWrap);
  }
}
```

---

### B3: day.html — 出勤登録ボタンを追加

index.html と同様、時刻入力セクション（`.time-group` 2つを含む `div.section`）の末尾に追加:
```html
        <button id="checkin-btn" class="btn-checkin-today">出勤登録（デフォルト時刻をセット）</button>
```

---

### B4: day.js — timeStep適用・renderItemRowsカテゴリ対応・checkin-btn追加

**① renderFields() 内でtimeStepをtime inputに適用**
`timeStartEl.value = ...` の前に追加:
```js
var step = (data.settings.timeStep || 1) === 15 ? 900 : 60;
timeStartEl.setAttribute('step', step);
timeEndEl.setAttribute('step', step);
```

**② renderItemRows() をカテゴリ対応に更新**

index.jsのB2⑥と同じ実装（ただし `todayKey` → `dateKey`、`data.records[todayKey]` → `data.records[dateKey]`）

**③ DOM refs に checkin-btn を追加**
```js
var checkinBtnEl = document.getElementById('checkin-btn');
```

**④ bindEvents() に checkin-btn のロジックを追加**
```js
if (checkinBtnEl) {
  checkinBtnEl.addEventListener('click', function() {
    var start = data.settings.defaultStartTime || '';
    var end   = data.settings.defaultEndTime   || '';
    if (!start && !end) {
      alert('デフォルト出勤・退勤時刻が設定されていません。設定画面から登録してください。');
      return;
    }
    var rec = ensureRecord(data, dateKey);
    if (start) rec.startTime = start;
    if (end)   rec.endTime   = end;
    persistData(data);
    renderFields();
    updateTotals();
  });
}
```

---

## Cチーム担当

### C1: settings.html — defaultEndTime追加・品目UIをカテゴリ別に分割

**① 出勤設定セクションに「デフォルト退勤時刻」を追加**

既存の `default-start-time` の `time-group` の直後に追加:
```html
      <div class="time-group" style="margin-top:8px;">
        <span>デフォルト退勤時刻</span>
        <input type="time" id="default-end-time">
      </div>
```

**② 品目セクションをカテゴリ別に分割**

現在:
```html
    <div class="section">
      <div class="section-title">品目一覧</div>
      <div id="items-list"></div>
      <button id="add-item" class="add-item-btn">+ 品目を追加</button>
    </div>
```

変更後:
```html
    <div class="section">
      <div class="section-title">品目一覧</div>

      <div class="item-category-label">キャストメニュー</div>
      <div id="items-list-cast"></div>
      <button id="add-item-cast" class="add-item-btn">+ キャストメニューを追加</button>

      <div class="item-category-label">シャンパン類</div>
      <div id="items-list-champagne"></div>
      <button id="add-item-champagne" class="add-item-btn">+ シャンパンを追加</button>
    </div>
```

---

### C2: settings.js — defaultEndTime・品目カテゴリ対応

**① DOM refs 変更**
- `var defaultStartEl = ...` の後に追加: `var defaultEndEl = document.getElementById('default-end-time');`
- `var itemsListEl = document.getElementById('items-list');` を以下に変更:
```js
var itemsListCastEl  = document.getElementById('items-list-cast');
var itemsListChampEl = document.getElementById('items-list-champagne');
```
- `var addItemBtn = document.getElementById('add-item');` を以下に変更:
```js
var addItemCastBtn  = document.getElementById('add-item-cast');
var addItemChampBtn = document.getElementById('add-item-champagne');
```

**② init() に defaultEndTime 初期化を追加**
`defaultStartEl.value = ...` の後に:
```js
if (defaultEndEl) defaultEndEl.value = data.settings.defaultEndTime || '';
```

**③ renderItems() をカテゴリ別に更新**
```js
function renderItems() {
  itemsListCastEl.innerHTML  = '';
  itemsListChampEl.innerHTML = '';
  data.settings.items.forEach(function(item, index) {
    var targetEl = item.category === 'champagne' ? itemsListChampEl : itemsListCastEl;
    var div = document.createElement('div');
    div.className = 'item-row';
    div.dataset.index = index;
    div.innerHTML =
      '<input type="text" class="item-name-input" value="' + escapeHtml(item.name) + '" placeholder="品目名" data-field="name">' +
      '<span class="back-label">バック</span>' +
      '<input type="number" class="item-back-input" value="' + (item.back || 0) + '" placeholder="単価" min="0" step="1" data-field="back">' +
      '<span class="back-label">円</span>' +
      '<button class="btn-delete" data-index="' + index + '" aria-label="削除">削除</button>';
    targetEl.appendChild(div);
  });
}
```

**④ bindEvents() の addItemBtn を2つのボタンに変更**
`addItemBtn.addEventListener('click', ...)` を削除し、以下に変更:
```js
addItemCastBtn.addEventListener('click', function() {
  data.settings.items.push({ id: generateId(), name: '新品目', back: 0, category: 'cast' });
  persistData(data);
  renderItems();
  var inputs = itemsListCastEl.querySelectorAll('.item-name-input');
  if (inputs.length) inputs[inputs.length - 1].focus();
});
addItemChampBtn.addEventListener('click', function() {
  data.settings.items.push({ id: generateId(), name: '新シャンパン', back: 0, category: 'champagne' });
  persistData(data);
  renderItems();
  var inputs = itemsListChampEl.querySelectorAll('.item-name-input');
  if (inputs.length) inputs[inputs.length - 1].focus();
});
```

**⑤ itemsListEl の click/change/blur イベントを2つに分ける**
`itemsListEl.addEventListener(...)` を `itemsListCastEl` と `itemsListChampEl` それぞれに適用:
```js
[itemsListCastEl, itemsListChampEl].forEach(function(listEl) {
  listEl.addEventListener('click', function(e) { /* 既存の削除ロジックそのまま */ });
  listEl.addEventListener('change', function(e) { /* 既存の変更ロジックそのまま */ });
  listEl.addEventListener('blur', function(e) { /* 既存のblurロジックそのまま */ }, true);
});
```

**⑥ bindEvents() に defaultEndTime イベントを追加**
`defaultStartEl.addEventListener(...)` の後に:
```js
if (defaultEndEl) {
  defaultEndEl.addEventListener('change', function() {
    data.settings.defaultEndTime = this.value;
    persistData(data);
  });
}
```

---

## 分担まとめ

| チーム | Engineer | ファイル |
|--------|---------|---------|
| A | 1 | style.css |
| A | 2 | common.js |
| B | 4 | index.html |
| B | 5 | index.js |
| B | 6 | day.html + day.js |
| C | 7 | settings.html |
| C | 8 | settings.js |

*作成: 2026-04-01*
